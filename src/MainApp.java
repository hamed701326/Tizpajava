public class MainApp {
    public static void main(String[] args) {
        User user=new User();
        Trip trip=new Trip();
        Distance distance=new Distance(ori)
        user.order(trip,distance);
    }
}
